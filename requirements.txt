# Direct runtime dependencies only — transitive ones are resolved by pip.
# Regenerate a full lockfile with: pip freeze > requirements.lock.txt

# --- Web framework ---
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-multipart==0.0.22
python-dotenv==1.2.2

# --- Persistence ---
SQLAlchemy==2.0.48
alembic==1.18.4

# --- LLM / agent orchestration ---
groq==1.1.2
langgraph==1.1.3

# --- RAG ---
# sentence-transformers pulls in torch (~2.5GB with CUDA wheels). For a
# CPU-only install, first run:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
chromadb==1.5.5
sentence-transformers==5.3.0
numpy==2.4.3

# --- Document parsing (knowledge ingestion) ---
pypdf==6.9.2
python-docx==1.2.0

# --- HTTP (weather tool) ---
requests==2.33.0
